<div class="form-field">
    <%= f.label :name, "Group Name:"%> </br> 
    <%= f.text_field :name, :value => (group.nil? ? '' : group.name), :required => true %>
 </div>
<div class="form-field">
	<%= f.label :description, "Group Description:" %> </br> 
	<%= f.text_area :description, :value => (group.nil? ? '' : group.description), :maxlength => 300 %>
</div>